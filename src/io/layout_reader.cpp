#include "layout_reader.h"
#include <fstream>
#include <sstream>

namespace io {

core::Layout read_layout(const std::string& path) {
  // TODO: Implement according to contest layout format.
  // Skeleton: returns empty layout to let the project compile & run.
  (void)path;
  return {};
}

} // namespace io
