#pragma once
#include <unordered_map>
#include <vector>
#include "../core/types.h"

namespace geom {
struct GridIndex {
  // skeleton; fill with buckets for acceleration
  void clear() {}
};
} // namespace geom
